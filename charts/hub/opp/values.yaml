# No Values
# Need to use fromSecret for these keys. problems ith json output.
##  accessKey: '{{ `{{ fromSecret "openshift-storage" "noobaa-admin" "AWS_ACCESS_KEY_ID" }}` }}'
##  secretKey: '{{ `{{ fromSecret "openshift-storage" "noobaa-admin" "AWS_SECRET_ACCESS_KEY" }}` }}'

# Managed cluster names for the DR pair (used by SSL extraction, precheck, and ArgoCD health monitor).
# Defaults here; override in values-hub.yaml (single source of truth for both opp-policy and rdr).
managedClusters:
  primaryName: ocp-primary
  secondaryName: ocp-secondary

argocdHealthMonitor:
  enabled: true

